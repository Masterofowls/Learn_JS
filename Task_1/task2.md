Задача 1 (Арифметика и приведение типов)

Что выведет код? Напишите ответ без запуска.

console.log(5 + '5');
console.log(5 - '5');
console.log(5 == '5');
console.log(5 === '5');
---

Задача 2 (Логические операторы)

Чему равно значение переменной result?

let a = 0;
let b = 'Hello';
let c = null;

let result = a || b && c;
console.log(result);
---

Задача 3 (Цикл for)

Сколько раз выполнится тело цикла и что будет в консоли?

for (let i = 0; i < 5; i++) {
  if (i === 3) break;
  console.log(i);
}
---

Задача 4 (Работа с массивом)

Дан массив. Напишите функцию, которая возвращает новый массив, содержащий только четные числа из исходного.

function getEvenNumbers(arr) {
  // Ваш код здесь
}

console.log(getEvenNumbers([1, 2, 3, 4, 5, 6])); // Должно вывести [2, 4, 6]
(Подсказка: используйте цикл for или метод push)

---

Задача 5 (Условия и функции)

Напишите функцию, которая принимает возраст пользователя. Если возраст меньше 18, возвращает "Доступ запрещен". Если 18 или больше — "Добро пожаловать!".

function checkAge(age) {
  // Ваш код здесь
}
---

Задача 6 (Строки)

Что выведет код?

let str = 'JavaScript';
console.log(str.length);
console.log(str[0]);
console.log(str.toUpperCase());
---

Задача 7 (Логическая ошибка)

Почему этот код работает не так, как задумано? Как исправить, чтобы он выводил числа от 10 до 1?

let i = 10;
while (i > 0) {
  console.log(i);
  i--;
}
(Код уже правильный, но вопрос: если бы было написано while (i) — что бы пошло не так?)

---

Задача 8 (Обмен значениями)

Как поменять значения переменных x и y местами без использования третьей переменной?

let x = 5;
let y = 10;

// Напишите код здесь

console.log(x); // 10
console.log(y); // 5