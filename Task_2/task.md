[8/2/2026 1:56 PM] Ivan: Тема 1: Создание, чтение, изменение и удаление свойств

Задача 1.1 (Базовое управление)
Создайте объект user с полями: name: "Айжан", age: 25, isAdmin: false.

1. Добавьте свойство city со значением "Астана".
2. Измените age на 26.
3. Удалите свойство isAdmin.
4. Выведите итоговый объект в консоль.

Задача 1.2 (Динамические ключи)
Напишите функцию buildObject(key, value), которая принимает ключ и значение и возвращает новый объект с этим ключом и значением. Используйте квадратные скобки.

Задача 1.3 (Проверка свойств)
Дан объект car = { brand: "Toyota", year: 2020, color: "red" }. Напишите функцию hasProp(obj, prop), которая возвращает true, если свойство есть в объекте (используйте оператор in), и false в противном случае.



---

Тема 2: Методы объекта

Задача 2.1 (Простые методы)
Создайте объект bankAccount с полями:

· owner: "Ержан"
· balance: 1000
· Метод deposit(amount) — увеличивает баланс на amount и возвращает новый баланс.
· Метод withdraw(amount) — уменьшает баланс на amount и возвращает новый баланс.
· Метод showBalance() — выводит строку: "Владелец: {owner}, Баланс: {balance}".

Задача 2.2 (Цепочки методов)
Создайте объект counter с полем value: 0 и методами:

· increment() — увеличивает value на 1 и возвращает this.
· decrement() — уменьшает value на 1 и возвращает this.
· getValue() — возвращает текущее value.
  Реализуйте цепочку: counter.increment().increment().decrement().getValue() — должно вернуть 1.

Задача 2.3 (Метод с данными)
Создайте объект team с полями:

· name: "Astana United"
· players: ["Али", "Дамир", "Рустем"]
· Метод addPlayer(name) — добавляет игрока в массив и возвращает обновленный массив.
· Метод showTeam() — выводит строку: "Команда {name}: {игроки через запятую}".

---

Тема 3: Цикл for...in

Задача 3.1 (Подсчет)
Дан объект grades = { math: 5, physics: 4, chemistry: 3, biology: 5 }.
С помощью for...in вычислите:

· Количество предметов
· Средний балл

Задача 3.2 (Фильтрация чисел)
Дан объект: mixed = { a: 10, b: "text", c: 25, d: "hello", e: 30, f: true }.
С помощью for...in создайте новый объект numbersOnly, содержащий только числовые свойства.

Задача 3.3 (Преобразование)
Дан объект prices = { apple: 100, banana: 50, orange: 80 }.
Создайте новый объект, где все цены будут увеличены на 20%.

---

Тема 4: Копирование объектов

Задача 4.1 (Поверхностное копирование)
Дан объект:

const original = {
  name: "Книга",
  details: {
    pages: 300,
    language: "Русский"
  }
};
Создайте копию copy тремя способами: спред-оператор, Object.assign(), цикл for...in.
Измените copy.name = "Журнал" и copy.details.pages = 150.
Выведите original. Объясните (в комментарии), почему изменилось pages, но не изменилось name.

Задача 4.2 (Глубокое копирование)
Напишите функцию deepCopy(obj), которая делает полную глубокую копию объекта (все вложенные объекты копируются по значению). Используйте рекурсию. Проверьте на объекте из задачи 4.1.

Задача 4.3 (Объединение)
Напишите функцию merge(obj1, obj2), которая возвращает новый объект, объединяющий свойства обоих объектов. При совпадении ключей приоритет у obj2. Исходные объекты не должны меняться.

---

Тема 5: Контекст this (только в методах объектов и стрелочные функции)

Задача 5.1 (Обычный метод)
Создайте объект person с полем name: "Данияр" и методом introduce(), который выводит "Меня зовут {this.name}".
Вызовите этот метод напрямую.

Задача 5.2 (Потеря контекста)
Создайте объект robot с полем model: "T-800" и методом getModel(), который возвращает this.model.
Затем:

1. Сохраните метод в переменную getModelFunc = robot.getModel.
2. Вызовите getModelFunc().
3. Объясните (в комментарии), почему результат отличается от ожидаемого.

Задача 5.3 (Стрелочная функция как метод)
Создайте объект obj с полем value: 100 и двумя методами:

· regularMethod() — обычная функция, возвращающая this.value.
· arrowMethod: () => this.value — стрелочная функция.
  Вызовите оба метода. Объясните разницу в результатах.
[8/2/2026 1:56 PM] Ivan: Задача 5.4 (Стрелочная функция внутри метода)
Создайте объект timer с полем seconds: 0 и методом start().
Внутри метода start() запустите setInterval:

· Вариант A: Используйте стрелочную функцию, которая увеличивает seconds каждую секунду и выводит его.
· Вариант B: Используйте обычную функцию.
  Какой вариант работает корректно и почему? Проверьте оба.

Задача 5.5 (Вложенные вызовы)
Создайте объект processor с полем data: 42 и методом process().
Внутри process() создайте внутреннюю функцию helper(), которая пытается вывести this.data.

· Сначала определите helper как обычную функцию и вызовите её внутри process().
· Затем определите helper как стрелочную функцию.
  Объясните, в каком случае this.data доступен, а в каком — нет.

Задача 5.6 (Колбэк в массиве)
Создайте объект collection с полем items: [1, 2, 3] и методом multiply(factor).
Внутри метода используйте forEach для умножения каждого элемента на factor.
Попробуйте два подхода внутри forEach:

· Использовать стрелочную функцию.
· Использовать обычную функцию.
  В обоих случаях результат должен сохраняться в новом массиве result (свойстве объекта). Какой подход сработает и почему?

---

Бонусная задача (всё вместе)

Задача 6 (Управление магазином)
Создайте объект shop с полями:

· name: "Супермаркет"
· products: [] — массив товаров (каждый товар — объект с полями title и price)
· Метод addProduct(title, price) — добавляет товар в массив и возвращает обновленный массив.
· Метод listProducts() — выводит все товары в формате "Название: {title}, Цена: {price}тг".
· Метод getExpensive() — возвращает массив товаров дороже 1000 тг (используйте for...in для перебора массива).
· Метод applyDiscount(discount) — уменьшает цену всех товаров на указанный процент (например, 10 = скидка 10%).

Проверка контекста:

1. Вызовите метод listProducts() напрямую.
2. Сохраните listProducts в переменную и вызовите её. Что произошло и почему?
3. Внутри метода applyDiscount используйте forEach для изменения цен.
   · Сделайте версию с обычной функцией внутри forEach.
   · Сделайте версию со стрелочной функцией.
   Какая из них правильно изменит цены?
